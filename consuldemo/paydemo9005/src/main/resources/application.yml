server:
  port: 9005

spring:
  application:
    name: paydemo
    cloud:
      consul:
        host: localhost
        port: 8500
        discovery:
          service-name: ${spring.application.name}

